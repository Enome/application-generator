var bundle = {
  order: 0,
  required: true,
  name: 'Express.js',
  description: 'The framework',
  code: '// Create the app \n' +
        'var express = require("express"); \n' +
        'var app = express();'
};

module.exports = bundle;
